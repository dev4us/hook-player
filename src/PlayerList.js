import React from 'react';

const PlayerList = ({statePlayList}) => {
    return (
        <>
        {
            /*statePlayList.map((val, index) => (
                <img src={val[index].thumbnail} alt={index}/>
            ))*/
        }
        </>
    )
}

export default PlayerList;